import javax.swing.*;
import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;

public class Driver {
    public static void main(String[] args) throws SQLException {
        //new FoodOrder(true);
        Choose choose = new Choose();
        //new EmployeeRecord();
        //MainMenu menu=new MainMenu();
        //MainMenuForEmpolyee m=new MainMenuForEmpolyee();
        //RoomDetails r = new RoomDetails(true);
//        Connection con= ConnectionConfiguration.getConnection();
//        DatabaseHelper dbhelper=new DatabaseHelper(con);
//        dbhelper.createFoodTable();
//        dbhelper.addFood("Ice Cream","150","Deserts");
        //System.out.println(dbhelper.getRoomRecord("1").toString());
        //new RoomBooking(true);
        //new BillGenerate(true);
        //ArrayList<Room> ar=new ArrayList<>(dbhelper.getAllRooms());
        //for(int i=0;i<ar.size();i++) {
        //    System.out.println(ar.get(i).toString());
       // }
    }
}
